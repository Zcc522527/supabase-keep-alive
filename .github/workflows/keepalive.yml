name: Supabase Keepalive

on:
  schedule:
    - cron: '0 0 * * *'  # 每天 UTC 0点运行
  workflow_dispatch:  # 允许手动触发

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Endpoint
        run: |
          curl -f "https://supabase-keep-alive-roan.vercel.app/api?key=${{ secrets.ACCESS_KEY }}" || exit 1
